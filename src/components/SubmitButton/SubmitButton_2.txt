import React from 'react';

const SubmitButton = ({ handleSubmit, isFormValid }) => {
  return (
    <button onClick={handleSubmit} disabled={!isFormValid}>
      Submit
    </button>
  );
};

export default SubmitButton;